# ==============================================================================
# 🚀 SIRINTHANATTH PRIME - Enterprise Requirements (v4.0.0)
# ปรับแต่งสำหรับ Python 3.12, FastAPI Async Architecture และ Google Cloud Run
# ==============================================================================

# --- 1. Core Web Framework & Server (ระบบแกนกลาง) ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0   # ⚡ อัปเกรด! ใส่ [standard] เพื่อดึง uvloop (C-based) รันเซิร์ฟเวอร์เร็วขึ้น 200%
orjson>=3.9.0               # ⚡ เพิ่มใหม่! ระบบอ่าน/เขียน JSON ที่เร็วที่สุดในโลก (เร็วกว่า json ปกติของ Python 3 เท่า)
pydantic>=2.6.0
pydantic-settings>=2.2.1    # ⚡ เพิ่มใหม่! จัดการ Environment Variables ให้ปลอดภัยและเป็นระเบียบสำหรับ Pydantic V2
python-dotenv>=1.0.1

# --- 2. Network & API Clients (ระบบเชื่อมต่อเครือข่าย) ---
httpx>=0.27.0               # ⚡ เพิ่มใหม่! ระบบ Async HTTP แบบใหม่ (ใช้แทน requests เพื่อแก้ปัญหา Timeout เวลาคุยกับ LINE)
requests>=2.31.0            # (เก็บไว้สำหรับ Library เก่าๆ ที่ยังต้องใช้)
aiohttp>=3.9.3              # จำเป็นสำหรับระบบ Async ภายในบางตัว

# --- 3. Third-Party Integrations (ระบบเชื่อมต่อภายนอก) ---
line-bot-sdk>=3.0.0         # ล็อกเวอร์ชันใหม่ล่าสุด รองรับ Async Webhook สมบูรณ์แบบ
stripe>=8.0.0               # ล็อกเวอร์ชันใหม่ล่าสุด รองรับระบบการเงินที่เสถียร

# --- 4. Database & Caching (ระบบคลังข้อมูลและความจำ) ---
supabase>=2.3.0
redis>=5.0.0
upstash-redis>=1.0.0

# --- 5. AI & Machine Learning (แกนสมองกล AI) ---
google-generativeai>=0.4.0  # (Legacy Gemini SDK เผื่อมีโค้ดเก่าใช้)
google-genai>=0.1.1         # New Official Gemini SDK (สำหรับ Gemini 3.7)
elevenlabs>=1.0.0           # Voice AI ขั้่นสูง

# --- 6. Google Cloud APIs (ระบบคลาวด์) ---
google-api-python-client>=2.100.0
google-auth>=2.25.0
google-cloud-secret-manager>=2.20.0

# --- 7. Media Processing & Utils (ระบบจัดการภาพ เสียง และวิดีโอ 4K) ---
moviepy>=1.0.3              # ระบบตัดต่อวิดีโอ
gTTS>=2.5.1                 # ระบบแปลงข้อความเป็นเสียงพื้นฐาน
Pillow>=10.2.0              # ระบบประมวลผลรูปภาพความละเอียดสูง
beautifulsoup4>=4.12.3      # ระบบดึงข้อมูลจากเว็บไซต์ (Web Scraping)

google-cloud-secret-manager
google-cloud-tasks